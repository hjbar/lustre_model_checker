node Mod3() returns (a : int);
let
  a = 0 -> ((pre a + 1) mod 3);
tel

node check() returns (OK : bool);
var a : int;
let
  a  = Mod3();

  -- Guard the first 3 instants so the property is defined from init
  OK = true -> true -> true -> (a = pre (pre (pre a)));
tel
